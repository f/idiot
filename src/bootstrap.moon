require 'idiot'

every second!, -> toggle pin 4

--led = pin 4
--other_led = pin 0

--blinking = blink led, second(0.5)

--every second(1), -> toggle other_led

--wait second(5), ->
--  stop blinking
--  close led
--  close other_led

